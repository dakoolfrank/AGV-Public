{
  "installCommand": "rm -f ../pnpm-workspace.yaml && npx pnpm@9 install --no-frozen-lockfile",
  "crons": [
    {
      "path": "/api/cron/export-referrals",
      "schedule": "0 0 * * 1"
    }
  ]
}

