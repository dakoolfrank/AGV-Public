'use client';

// This dynamic route allows /buy/wallet/[address] to work
// The buy page component will extract the wallet address from the pathname
import BuyPage from '../../page';

export default BuyPage;

